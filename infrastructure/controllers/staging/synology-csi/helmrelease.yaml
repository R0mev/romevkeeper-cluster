apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: synology.csi
  namespace: synology.csi
spec:
  interval: 1h
  chart:
    spec:
      chart: synology-csi
      version: "0.11.2"
      sourceRef:
        kind: HelmRepository
        name: synology-csi
        namespace: flux-system
  values:
    clientInfoSecret:
      name: NAS-info-secret
